# CI Environment Variables (ephemeral credentials for testing only)
# Server
PORT=3000
HOST=0.0.0.0
NODE_ENV=production

# Database (PostgreSQL)
PGHOST=localhost
PGPORT=5432
PGDATABASE=smart_inventory
PGUSER=postgres
PGPASSWORD=postgres
PGSSL=false

# Auth (JWT)
JWT_SECRET=ci-test-secret-do-not-use-in-production-12345678901234567890123456789012
JWT_ISSUER=smart-inventory
JWT_EXPIRES_IN=12h

# Simple credentials for CI token issuance
VIEWER_USERNAME=viewer
VIEWER_PASSWORD=viewer123
OPERATOR_USERNAME=operator
OPERATOR_PASSWORD=operator123

# MLflow
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_EXPERIMENT_NAME=smart-inventory
